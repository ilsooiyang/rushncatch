<%= form_for([@player, @blog]) do |f| %>
	<%= render "shared/errors" %>
	<p>
	<%= f.label :content %><br />
	<%= f.text_area :content, cols: 100, rows: 20 %>
	</p>
	<p>
	<%= f.submit %> |
	<%= link_to("Cancel", player_blogs_path(@player)) %>
	</p>
<% end %>